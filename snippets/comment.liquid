<!-- /snippets/comment.liquid -->
{% comment %}

This snippet generates the structure of a comment.
It is used to show not only recent comments, but also comments that were
just published but not yet approved.

More information:
- http://docs.shopify.com/themes/liquid-variables/comment

{% endcomment %}

<div class="author-thought table">
  <div class="ava-author">
    <a href="/"><img src="//placehold.it/70x70" alt="author" /></a>
  </div>
  <div class="content-box">
    <div class="name-author clearfix">
      <h3 class="title14 text-uppercase font-bold pull-left">{{ comment.author }} {{ 'blogs.article.says' | t}}:</h3>
      <a href="#comments" class="link-rep font-semi-bold link-hover text-uppercase pull-right">{{ 'blogs.comments.reply' | t}}</a>
    </div>
    <p class="desc">{{ comment.content }}</p>
    <a href="/" class="link-hover title12 silver">{{ comment.created_at | date: '%b %d, %Y' }} </a>
  </div>
</div>